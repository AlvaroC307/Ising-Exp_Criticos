<<<<<<< HEAD
include("Medidas.jl")
using Random

#---------------------------------------------------------------------------------------------------------------------
#---------------------------------------------------------------------------------------------------------------------
#-------------------------------------------------CREAR RED-----------------------------------------------------------
#---------------------------------------------------------------------------------------------------------------------
#---------------------------------------------------------------------------------------------------------------------

#Función que crea una red NxN con los valores aleatorios eligiendo entre los que están entre paréntesis
red_random(N) = rand((-1,1),N,N)

#También se puede poner asi
#= function red_random(N)
    red = rand((-1,1),N,N)
    return red
end =#

#---------------------------------------------------------------------------------------------------------------------
#---------------------------------------------------------------------------------------------------------------------
#------------------------------------------------METRÓPOLIS-----------------------------------------------------------
#---------------------------------------------------------------------------------------------------------------------
#---------------------------------------------------------------------------------------------------------------------

function Metropolis(red, N, vec_exp)  
    # Escoge un elemento de la red aleatorio, evalúa la función mover, 
    #si es True realiza el cambio y calcula la variación de la energía

    RND_x = rand(1:N)
    RND_y = rand(1:N)

    if mover(RND_x, RND_y, red, N, vec_exp) == true
        red[RND_x, RND_y] = -1*red[RND_x, RND_y]
    end
    
    return nothing
end

#---------------------------------------------------------------------------------------------------------------------
#---------------------------------------------------------------------------------------------------------------------
#---------------------------------------------------MOVER-------------------------------------------------------------
#---------------------------------------------------------------------------------------------------------------------
#---------------------------------------------------------------------------------------------------------------------

function mover(x, y, red, N, vec_exp)  # Función que decide si cambiar o no el spin red[x, y]
    rnum = rand() #Numero aleatorio entre 0 y 1
    ssuma = SpinSuma(x, y, red, N)
    s0 = red[x, y]
    prob_mover = vec_exp[Int((s0+1)/2)+1, Int(ssuma/2)+3]

    if rnum <= prob_mover
        return true
    else
        return false
    end
end
=======
include("Medidas.jl")
using Random

#---------------------------------------------------------------------------------------------------------------------
#---------------------------------------------------------------------------------------------------------------------
#-------------------------------------------------CREAR RED-----------------------------------------------------------
#---------------------------------------------------------------------------------------------------------------------
#---------------------------------------------------------------------------------------------------------------------

#Función que crea una red NxN con los valores aleatorios eligiendo entre los que están entre paréntesis
red_random(N) = rand((-1,1),N,N)

#También se puede poner asi
#= function red_random(N)
    red = rand((-1,1),N,N)
    return red
end =#

#---------------------------------------------------------------------------------------------------------------------
#---------------------------------------------------------------------------------------------------------------------
#------------------------------------------------METRÓPOLIS-----------------------------------------------------------
#---------------------------------------------------------------------------------------------------------------------
#---------------------------------------------------------------------------------------------------------------------

function Metropolis(red, N, vec_exp)  
    # Escoge un elemento de la red aleatorio, evalúa la función mover, 
    #si es True realiza el cambio y calcula la variación de la energía

    RND_x = rand(1:N)
    RND_y = rand(1:N)

    if mover(RND_x, RND_y, red, N, vec_exp) == true
        red[RND_x, RND_y] = -1*red[RND_x, RND_y]
    end
    
    return nothing
end

#---------------------------------------------------------------------------------------------------------------------
#---------------------------------------------------------------------------------------------------------------------
#---------------------------------------------------MOVER-------------------------------------------------------------
#---------------------------------------------------------------------------------------------------------------------
#---------------------------------------------------------------------------------------------------------------------

function mover(x, y, red, N, vec_exp)  # Función que decide si cambiar o no el spin red[x, y]
    rnum = rand() #Numero aleatorio entre 0 y 1
    ssuma = SpinSuma(x, y, red, N)
    s0 = red[x, y]
    prob_mover = vec_exp[Int((s0+1)/2)+1, Int(ssuma/2)+3]

    if rnum <= prob_mover
        return true
    else
        return false
    end
end
>>>>>>> 95bbd10921ed654fbb4ecc8bb3d11b30c27ee8b8
